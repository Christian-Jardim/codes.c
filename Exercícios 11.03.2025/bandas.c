#include <stdio.h>
#include <string.h>

typedef struct {
	char nome[30];
	char gen[20];
	int rank;
	int integ;
}Band;


int main() {
	Band band[5];
}

void ran(int r) {
	for(int i=0; i<5; i++) {
		if(pos.x[i] == r) {
			printf("\nBanda:%s\n",band.x[i]);
			printf("Estilo musical:%s\n",gen.x[i]);
			printf("Quantidade de integrantes:%d\n",integ.x[i]);
printf("Lugar no ranking:%d\n",pos.x[i]);
		}
	}
}